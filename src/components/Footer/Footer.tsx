import "./Footer.scss";

function Footer() {
  return (
    <footer className="footer">
      © {new Date().getFullYear()} PROSPERI — Construcción de alto estándar
    </footer>
  );
}

export default Footer;
