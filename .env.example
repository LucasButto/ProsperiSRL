# Contact form (Web3Forms). Get a free key at https://web3forms.com using the
# email that should receive the messages, then copy this file to .env.local
# for local dev and add the same variable in Vercel > Settings > Environment Variables.
VITE_WEB3FORMS_ACCESS_KEY=
